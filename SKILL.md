---
name: ya-pai
description: 组合式文章排版 skill：把 Markdown 文章排版为单个自包含、可直接复制进微信公众号编辑器的 HTML 文件。风格由「气质 × 色板 × 字体 × 版式骨架 × 疏密 × 装饰 × 背景样式 × 功能模块 × 标题风格」九维度自由组合而成，并内置 12 套一键预设组合；版式骨架（单栏流/导读+章节流/时间线式/卡片式/海报式/侧栏式/讲义式/报章式/信笺式/终端式）决定整篇文章的第一眼印象，标题风格（编号细线/水印数字/渐变圆角徽标/反白通栏/报章方块等）可独立更换。排版前向用户确认——效果优先：先看组合画廊、再确认 Agent 推荐、最后单点微调；每个维度同时提供预设选项与自由填写两条路，Agent 只给推荐项、不得替用户选定；仅当用户明确说“全部按推荐 / 自动 / 你定”时才可直接采用推荐组合。当用户提到“公众号排版”“文章排版”“把 md 排版成 HTML”“排版美化”“文章样式设计”“雅排”，或要求生成可直接粘贴到公众号编辑器的 HTML 时使用。不用于生成普通网页、落地页、PPT 或邮件排版。
---

# 雅排（ya-pai）：组合式公众号文章排版

把一篇 Markdown 文章排版为**单个自包含、可直接复制进微信公众号编辑器的 HTML 文件**。风格由九个维度组合而成：气质、色板、字体、版式骨架、疏密、装饰、背景样式、功能模块、标题风格。排版前先向用户确认组合；用户说「自动 / 直接排 / 不用问」时跳过提问，按文章「题材 × 文风」自动推荐。

不用于：普通网页 / 落地页 / PPT / 邮件排版。

## 核心资产

- `tokens/`：九维度的可选零件（气质、色板、字体、版式骨架、疏密、背景样式、标题风格指引；装饰与功能模块规格见 references/design-system.md）＋ 12 套一键预设组合（presets.md）
- `references/`：排版规则（设计系统总览、禁项、中英文排版细则、版式说明、输出规范、**编辑级审美原理 editorial-principles.md**、**维度驱动主题组件库 component-library.md**）
- `scripts/`：校验脚本 validate_gzh_html.py + 预览包装 wrap_preview.py + **发布态审计 publish_audit.py**（**强制使用**）
- `assets/`：预览页模板（wrap_preview.py 依赖）
- `examples/gallery/`：**组合画廊**——11 套代表性组合用同一篇短文渲染，让用户「看图选型」
- `examples/`：示例（sample-article.md → sample-quiet.html 为旧版整页示例，仅参考气质；产物格式以 output-spec.md 为准）

## 工作流程

### 1. 确定输入与内容边界

- 输入为 `.md` 路径或直接粘贴的 Markdown 文本；非 Markdown（docx/pdf/txt）先简单归一化为 Markdown。
- **不改动原文实质内容**：不增删事实、不重写句子、不添加原文没有的结论。只允许：规范化标点、修整换行、为无标题长文补标题层级（交付时说明补了什么）。

### 2. 询问组合（必须，不可跳过）——效果优先，预设可选、自定义亦可

九个维度（气质、色板、字体、版式骨架、疏密、装饰、背景样式、功能模块、标题风格）全部由用户选定。你可以推荐，但**不能替用户选择**。流程按 references/design-system.md 第 10 节；**关键决策点（画廊选择、推荐确认、最终确认）必须用「带选项的提问」问用户，禁止发完链接/发完推荐就结束回合让用户自己打字**。

1. **看画廊 + 画廊选择提问**：打开 examples/gallery/index.html（11 套，编号 g01–g11 显示在每张卡和每个预览页的徽标上），**紧接着用提问工具问「哪一套接近你的感觉」**——选项 = 11 套编号（推荐套标「🔹 Recommended」+ 一句理由），用户点选编号即确认起点；不能只发链接然后结束回合。
2. **Agent 推荐整组 + 推荐确认提问**：先做「题材 × 文风」判定（不是只看题材——技术文章写得轻松就该推轻松的组合），**推荐理由必须包含文风判断**（如「判断型、中立、密集」）；陈述完推荐后**立刻用提问收口**（推荐组合 / 微调某维度 / 自由填写）；用户说「按推荐 / 自动 / 你定」→ 停止提问直接排版。
3. **单点微调**：用户只需说「标题换渐变圆角那种」「版式换海报式」「色板用黄白蓝」，Agent 按冲突消解表自动调和其余原子，交付时说明改了什么。
4. 每个维度仍保留 2–3 个预设选项（推荐项第一位、标「（Recommended）」、附一句理由）+「其他」自由填写通道：自定义内容要解析成具体 CSS 值（库内值直接套用；库外描述按平台红线实现），不得因「不在预设里」拒绝。
5. **最终确认（必须提问收口）**：复述九维度组合后，**用提问工具给出确认选项**（确认 / 微调气质 / 微调色板 / 微调字体 / 微调版式骨架 / 微调标题风格 / 其他），用户点选或填「其他」后再进入组装；未经用户选择不得用推荐值静默补齐。
6. **签名卡信息（必问，组装前最后一步）**：用提问工具获取**作者名 + 一句话简介**——options：「现在提供（推荐，在『其他』按『作者名；一句话简介』格式填写）」/「暂不提供，先留占位符」；用户明确不提供才允许 `{{作者名}}`/`{{一句话简介}}` 占位并在交付时提醒，**不得默认留占位而不问**；原文末尾已有署名段 → 并入签名卡，不重复生成。

**提问工具**：必须调用 Codex 原生提问工具（request_user_input / AskUserQuestion），每个问题 header ≤12 字符、question 一句话、2–3 个互斥预设选项，客户端会自动提供「其他」供自由填写。仅当环境确认没有该工具（例如 Default 模式被禁用）时：先明确告知用户「原生问题卡片需在 Plan 模式使用」，再按用户意愿处理——切到 Plan 模式后用原生卡片，或用户同意后用紧凑编号列表退化；**退化时也必须以「带全部选项的提问」结束并等待用户回复，不得自行替选，不得以开放式文本结束回合**。

### 3. 读取选定组合的零件

按需读取，只读命中的文件，不整库加载：

1. 先读 `references/design-system.md`（组合逻辑 + 功能模块规格 + 题材×文风推荐）、`references/anti-slop.md`（全程禁项）和 `references/editorial-principles.md`（组件预算 + 节奏 + 编辑心态）。
2. 按用户选择读 `tokens/aesthetics.md`、`tokens/color-palettes.md`、`tokens/font-pairings.md`、`tokens/layouts.md`、`tokens/density.md` 中命中的条目。
3. 再读 `references/typography.md`（逐元素细则）、`references/layout-patterns.md`（版式结构）、`references/output-spec.md`（输出与平台红线）。
4. 读 `references/component-library.md`（维度驱动设计系统：审美原理 + 组件族/变体 + 变量表）——**产物 HTML 一律从这份取，不凭记忆手写、不复制其它主题成品**。
5. 用户看过画廊 → 按用户指认的组合对应到 tokens/presets.md 的取值；没看画廊就按推荐组合排。

### 4. 组装 HTML（核心）

- 产物是**纯 `<section>…</section>` 正文片段**（无 `<!DOCTYPE>/<html>/<head>/<body>`），从组件库的全局容器开始。
- **HTML 一律从 references/component-library.md 取组件**（按「维度 → 变体选择表」与「气质签名」选变体），把 `{{占位}}` 替换为所选 token 的具体值；不凭记忆手写。
- **所有文字节点用 `<span leaf="">文字</span>` 包裹**；样式全部内联；禁 `<div>` / `class` / `id`。
- 每个 Markdown 元素按 component-library.md 的「Markdown → 组件映射规则表」映射；装饰/背景档位按「维度 → 组件选择表」选组件变体。
- 功能模块按 design-system.md 开关；sticky 目录 / 折叠在公众号版一律退化为静态卡片或省略（公众号不支持）。
- 按 output-spec.md 的「生成时的智能处理」执行：章节编号、英文标签、每段 1–3 处下划线、目录、全角标点。

### 5. 校验（强制）

把生成的正文 HTML 写入目标文件后，**必须运行**：

```bash
<SKILL_ROOT>/scripts/validate_gzh_html.py <生成的正文.html>
```

ERROR 清零才算完成；半角标点 WARNING 同样修复到 0 再交付。审美类 WARNING（连续 4+ 段纯文字 / AI 默认色 / font-size 超限）应处理：能修则修，确因原文结构无法修的在交付说明中注明人工确认。报 ERROR 就回到第 4 步修。

### 6. 输出与交付

1. 干净正文文件：`{原文文件名}_排版_{气质/预设标识}.html`，保存到与原文同目录。
2. 预览页：`<SKILL_ROOT>/scripts/wrap_preview.py <上面的正文.html>`，产出 `{...}_预览.html`（右上角「复制到公众号」按钮）。
3. 发布态审计（**交付前必跑**）：`<SKILL_ROOT>/scripts/publish_audit.py <上面的正文.html>`——占位符残留 / 本地图片 / 体积 / 预览页；❌ 项清零才算可发布。
4. 交付时给出：两个文件路径、组合说明（九维度最终选择，均经用户确认；推荐理由一句话）、粘贴指引（打开预览页 → 点「复制到公众号」→ 公众号编辑器粘贴）、校验结论与审计结论；若含本地图片，提示先上传公众号素材库再替换 `src`；标题在公众号后台设置。

## 维度速查

| 维度 | 选项 | 作用 | 详见 |
|---|---|---|---|
| 气质 | 安静编辑 / 现代极简 / 人文书卷 / 冷冽理性 / 温暖纸感 | 决定整体感觉 | tokens/aesthetics.md |
| 色板 | 暖米 / 冷灰 / 深色 / 高对比 / 低饱和 | 背景+文字+强调色 | tokens/color-palettes.md |
| 字体 | 衬线标题+无衬线正文 / 全无衬线 / 中文友好衬线 / 无衬线标题+衬线正文 / 等宽点缀 / 宋体报章 | 最影响「高级感」 | tokens/font-pairings.md |
| 版式骨架 | 单栏流 / 导读+章节流 / 时间线式 / 卡片式 / 海报式 / 侧栏式 / 讲义式 / 报章式 / 信笺式 / 终端式 | **决定第一眼印象与骨架形状** | tokens/layouts.md + references/layout-patterns.md |
| 疏密 | 松 / 中 / 紧 | 控制呼吸感 | tokens/density.md |
| 装饰 | 几乎无装饰 / 细线 / 轻微色块 / 强调引用 | 避免繁杂 | references/design-system.md |
| 背景样式 | 纯色 / 微渐变 / 顶部色带 / 深浅分层 / 细边框留白 / 三色分区 | 页面与内容区的底 | tokens/backgrounds.md |
| 功能 | sticky 目录 / 折叠 / 代码增强 / 图注风格 / 章节编号 / 结尾签名（强制） | 按内容需要开关；签名每篇必加 | references/design-system.md |
| 标题风格 | 编号细线 / 水印大数字 / 大数字+竖线 / 色块章头 / 墨线方块 / 反白通栏 / 渐变圆角徽标 / 衬线居中大字 / 终端标签 / 报章方块 | 章节标题的独立结构变体 | references/component-library.md F3 族 |

另有 **12 套一键预设组合**（tokens/presets.md）和 **组合画廊**（examples/gallery/index.html），可先看图选型、再微调。

## 平台红线（公众号）

1. 产物为纯 `<section>` 片段；无 `<!DOCTYPE>` / `<html>` / `<head>` / `<body>`。
2. 样式全内联；无 `<style>` / `<script>` / `<div>` / `<link>` / `class` / `id`。
3. 所有文字节点用 `<span leaf="">` 包裹。
4. 只用系统字体栈，不引用外部字体、CSS、JS；`font-size` ≤ 24px。
5. 不用 `position:fixed/absolute/sticky`、`float`、`@media`、`@keyframes`、`display:grid`、CSS 变量。
6. 图片 `max-width:100%; height:auto; display:block; margin:0 auto`。
7. 中文标点全角；代码、URL、英文保持半角。
8. 装饰性空元素内部放 `<span leaf=""><br></span>`。

## 扩展与维护

- 新色板 → 在 tokens/color-palettes.md 增加条目；新气质 → 在 tokens/aesthetics.md 增加条目并补默认组合；新标题风格 → 在 references/component-library.md F3 族增加条目并登记到「变体选择表」。
- 改输出规范 → 编辑 references/output-spec.md；改逐元素细则 → 编辑 references/typography.md。
- 保持 SKILL.md 精简：新细节尽量放进 references/ 或 tokens/，不要在 SKILL.md 里展开。
